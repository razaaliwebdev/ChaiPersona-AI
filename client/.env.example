# Backend server base URL (no trailing slash, no /api suffix)
# Required in production when client and server are separate services
#
# Development: leave empty — Vite proxy forwards /api to localhost:3000
# Production:  https://your-server-service.sevalla.app
VITE_API_URL=
